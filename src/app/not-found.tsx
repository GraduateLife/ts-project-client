import NotFound from '@/theme/ui/NotFound';
import React from 'react';

const NOT_FOUND = () => {
  return (
    <div className="mb-12">
      <NotFound />
    </div>
  );
};

export default NOT_FOUND;
