export type SiteUser = {
  userId: string;
  userName: string;
  accountId: string;
};

export type UserCheckOutAddress = {
  checkoutName: string;
  checkoutAddress: string;
  checkoutPhone: string;
};
